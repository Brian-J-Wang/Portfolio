---
import styles from "./sectionHeader.module.css";
const { title, subTitle } = Astro.props;
---

<div class={styles.header}>
	<h3 class={styles.header__title}>{title}</h3>
	<p class={styles.header__desc}>{subTitle}</p>
</div>

---
import styles from "./icon.module.css";

const { img, name } = Astro.props;
---

<div class="flex flex-col items-center gap-2">
	<img src={img} alt={name} class={styles.icon__image} />
	<p class={styles.icon__name}>{name}</p>
</div>
